{
  "rewrites": [
    { "source": "/sitemap.xml", "destination": "/api/sitemap" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
```
4. Click **Commit changes**

---

**Step 3 — Delete the old static `sitemap.xml`**

1. Click `sitemap.xml` in your repo
2. Click **three dots `...`** → **Delete file** → **Commit**

---

**Step 4 — Test it**

After Vercel redeploys (about 30 seconds), visit:
```
https://hearthpick.vercel.app/sitemap.xml
